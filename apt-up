#!/bin/bash
#
# Run apt-get commands to uplift the system:
#
#   * update
#   * upgrade
#   * clean, autoclean, autoremove with purge
#
# The upgrade uses:
#
#   * -y yes to everything
#   * -f force changes
#   * --fix-missing
#   * --install-recommends
#
set -o nounset
set -o errexit
set -o pipefail

## Scrub before uplift
apt-get autoclean
apt-get --purge autoremove

## Uplift
apt-get update
apt-get upgrade -y -f --fix-missing --install-recommends

## Scrub after uplift
apt-get autoclean
apt-get --purge autoremove
