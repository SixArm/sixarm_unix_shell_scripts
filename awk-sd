##
# Input a list of numbers, output the standard deviation.
##

awk '{ sum += $1; a[NR] = $1}
END {
  mean = sum/NR
  for ( x = 1; x <= NR; x++ ){
    sumsq += ( a[x] - mean ) ** 2;
  }
  print sqrt( sumsq/NR )
}'