##
# Input a list of numbers, output the min, mean, max, sum, and count.
##

awk 'NR == 1 { min = $1; max = $1; sum = 0 }
  { if ( $1 < min ) min = $1; if ($1 > max) max = $1; sum += $1; }
  END { print "min:", min, "mean:", sum/NR, "max:", max, "sum:", sum, "count:", NR}'
