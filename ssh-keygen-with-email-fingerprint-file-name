##
# SSH script to generate our typical key:
#
#   * rsa algorithm because it's the widest used.
#   * 4096 bytes of strength; default would be 2048.
#   * append a comment that is the user's email address.
#   * generate a ZUID to identify this key.
#   * set the file name to "<email>=<zuid>=id_rsa"
##

email=$1
zuid=$(uuid | tr 'A-Z' 'a-z' | sed 's/-//g')
echo "zuid:$zuid"
file="$email=$zuid=rsa"
echo "file:$file"
ssh-keygen -t rsa -b 4096 -C "$email" -f "$file"
